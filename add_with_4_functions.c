#include<stdio.h>
int inp(int,int);
int out(int,int);
int add(int,int);
int main()
{
  int a,b,c,d,sum;
  a=inp(b,c);
  printf(“enter two numbers”);
  scanf(“%d%d”,&b,&c);
  sum=add(b,c);
  d=out(b,c);
  printf(“%d is the sum”,sum);
}
int inp(int m,int n)
{
return m,n;
}
int add(int p,int q)
{
return(p+q);
}
int out(int x,int y);
{
return 0;
}
